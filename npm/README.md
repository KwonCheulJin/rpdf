# npm/

v0.4 WASM 빌드 산출물 예정 디렉토리.

`wasm-pack build` 실행 시 `@rpdf/core` npm 패키지가 여기에 생성될 예정.  
현재는 플레이스홀더 (v0.4 이전까지 비어 있음).

## 예상 구조 (v0.4)

```
npm/
  core/
    package.json
    rpdf_core_bg.wasm
    rpdf_core.js
    rpdf_core.d.ts
```
